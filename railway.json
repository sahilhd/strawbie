{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd youtube-backend && npm install"
  },
  "deploy": {
    "startCommand": "cd youtube-backend && node server.js",
    "restartPolicyMaxRetries": 5
  }
}

