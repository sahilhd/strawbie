{
  "name": "youtube-music-backend",
  "version": "1.0.0",
  "description": "Backend service to extract YouTube audio streams for music playback",
  "main": "server.js",
  "scripts": {
    "postinstall": "pip3 install yt-dlp || python3 -m pip install yt-dlp || true",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "youtube-dl-exec": "^2.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": "18.x"
  }
}

